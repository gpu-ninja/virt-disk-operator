#@ load("@ytt:overlay", "overlay")

#@overlay/match by=overlay.subset({"kind": "Deployment"})
---
metadata:
  #@overlay/match missing_ok=True
  annotations:
    kapp.k14s.io/change-group: "koopt.gpu-ninja.com/virt-disk"
    kapp.k14s.io/change-rule: "upsert after upserting koopt.gpu-ninja.com/mount-udev"
spec:
  template:
    spec:
      containers:
      #@overlay/match by=overlay.all
      - image: ghcr.io/gpu-ninja/virt-disk-operator:latest-dev